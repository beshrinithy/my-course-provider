// src/components/CourseInfo/Pricing.js
import React from 'react';

const Pricing = ({ course }) => {
    return (
        <div>
            {/* Code for displaying pricing */}
        </div>
    );
};

export default Pricing;
